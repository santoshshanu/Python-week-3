



names = ['abc', 'abcdef', 'harshit']
# 0 -- 'abc'
# 1 -- 'abcdef'
# pos = 0
# for name in names:
#     print(f"{pos} ----> {name}")
#     pos += 1


# with enumerate function

for pos, name in enumerate(names):
    print(f"{pos} ----> {name}")


# Define a function that take two arguments
# 1.) list containing string 
# 2.) string that want to find in your list
# and this function will be return the index of string in your list and
# if string is not present then return -1

def find_pos(l, target):
    for pos, name in enumerate(1):
        if name == target:
            return pos
    return -1

print(find_pos(names,'harshit'))